# 포트폴리오 면접 스크립트
## "내가 왜 이걸 만들었고, 무엇을 해결했고, 무엇을 배웠는지"

---

## 30초 엘리베이터 피치 (면접 첫 소개)

> "서울 아파트 실거래가 데이터를 국토부 공공 API에서 자동 수집해서
> SQLite DB에 저장하고, 통계 분석과 ML 가격 예측, Streamlit 대시보드,
> GPT 기반 AI 시장 해설까지 end-to-end로 혼자 구현한 데이터 파이프라인 프로젝트입니다.
> 단계별로 작동하는 걸 확인하며 만들었고, 현재 서울 25개 구 수만 건의 실거래 데이터가
> 쌓여 있으며 매월 1일 자동 수집됩니다."

---

## 왜 이걸 만들었나?

### 문제 인식
"부동산 가격이 궁금할 때마다 네이버, 호갱노노 등 여러 사이트를
돌아다니며 수동으로 확인해야 했어요.
그런데 이 데이터의 원본이 국토부에서 무료로 제공되는 공공 API라는 걸 알았고,
이걸 직접 수집해서 내가 원하는 방식으로 분석하고 싶다고 생각했습니다."

### 목표 설정
"처음부터 완벽한 시스템을 만들려 하지 않고
1단계(수집) → 2단계(저장) → 3단계(분석) → 4단계(시각화) → 5단계(AI 해설)
순서로 각 단계가 작동하면 다음으로 넘어가는 방식으로 접근했습니다."

---

## 어떤 문제를 해결했나?

### 기술적 문제 3가지

**문제 1. API 연동 오류**
"공공 API 문서에는 엔드포인트가 http로 나와 있었는데,
실제로는 https만 허용하고 있었습니다.
브라우저로 직접 API를 호출해서 응답을 확인하고,
실제 XML 태그명이 문서(한글)와 다르게 영문으로 반환된다는 것도 직접 확인해서 수정했습니다."

**문제 2. 중복 데이터 저장**
"같은 데이터를 여러 번 수집하면 DB에 중복이 쌓이는 문제가 있었습니다.
SQLite의 UNIQUE 제약 조건과 INSERT OR IGNORE를 조합해서
같은 (아파트명+면적+층+날짜+금액) 조합은 절대 중복 저장되지 않도록 설계했습니다.
이 덕분에 스케줄러가 매월 자동 실행해도 데이터가 깨지지 않습니다."

**문제 3. 카카오맵 SDK 차단**
"Streamlit의 iframe 보안 정책으로 인해
카카오맵 JavaScript SDK가 차단되었습니다.
Folium 라이브러리로 대체해서 Python에서 직접 HTML을 생성하는 방식으로
외부 API 의존 없이 지도 히트맵을 구현했습니다."

---

## 무엇을 배웠나?

### 기술적 배움

**1. 데이터 파이프라인 설계**
"수집 → 저장 → 분석 → 시각화의 각 단계를 별도 파일로 분리하는
'관심사 분리(Separation of Concerns)' 원칙을 실전에서 배웠습니다.
덕분에 지도 로직을 바꿔도 분석 코드를 건드리지 않아도 됩니다."

**2. 멱등성 있는 파이프라인**
"INSERT OR IGNORE + UNIQUE 제약으로
몇 번 실행해도 결과가 같은 멱등성(Idempotency) 파이프라인을 구현했습니다.
자동화 시스템에서 이게 얼마나 중요한지 직접 경험했습니다."

**3. 공공 API의 현실**
"문서와 실제 동작이 다를 수 있다는 걸 배웠습니다.
개발 환경(Dev) 엔드포인트와 정식 엔드포인트의 차이,
인코딩/디코딩 API 키의 차이 등 문서만 보고는 알 수 없는 것들을
직접 테스트하며 해결했습니다."

**4. 단계별 개발의 힘**
"처음부터 완성본을 만들려 했다면 중간에 포기했을 것 같습니다.
1단계가 작동하면 2단계, 이런 식으로 진행하니
각 단계의 성취감이 동기부여가 되었고, 결국 6단계까지 완성했습니다."

### 도메인 배움

"서울 아파트 시장에서 강남·서초·송파가 평균 20억 이상인 반면
노원·도봉은 5억 이하로, 같은 서울 안에서도 4배 이상의 격차가 있습니다.
40~60㎡ 소형 아파트의 ㎡당 단가가 대형보다 높다는 것도 데이터로 직접 확인했습니다."

---

## 기술 스택 선택 이유

| 기술 | 선택 이유 |
|------|-----------|
| Python | 데이터 처리에 가장 적합한 생태계 |
| SQLite | 서버 설치 없이 파일 하나로 작동, 나중에 PostgreSQL 마이그레이션 가능 |
| pandas | 표 형태 데이터 처리의 표준 라이브러리 |
| Streamlit | HTML/CSS/JS 없이 Python만으로 웹앱 구현 가능 |
| Folium | 외부 API 키 없이 지도 시각화 가능 |
| RandomForest | 비선형 관계 포착, 과적합에 강함, 특성 중요도 직관적 |
| APScheduler | Python 내에서 cron 스케줄링 가능 |
| OpenAI API | 데이터를 자연어로 해설하는 가장 쉬운 방법 |

---

## 숫자로 말하는 프로젝트 성과

- 수집 데이터: 서울 25개 구, 6개월치, **수만 건**
- 자동화: 매월 1일 자동 수집 + 주간/월간/분기/연간 자동 리포트
- 분석 항목: 구별/동별 통계, 면적별 단가, Z-score 이상치, ML 예측
- 대시보드: 7개 탭 (추세/동별/면적/단지검색/이상거래/지도/ML예측)
- 리포트: HTML 자동 생성 + GPT 해설 포함

---

## 예상 면접 질문 & 답변

**Q. 이 프로젝트에서 가장 어려웠던 점은?**
"API 문서와 실제 동작이 달랐던 것입니다.
http→https 이슈, XML 태그 한글/영문 불일치, Streamlit iframe 보안 제한 등
문서만으로는 알 수 없는 문제들이 있었고,
각각을 브라우저 직접 테스트, 개발자 도구 콘솔 확인, 라이브러리 교체 등으로 해결했습니다."

**Q. SQLite를 선택한 이유는? PostgreSQL은 왜 안 썼나요?**
"프로젝트 초기엔 SQLite가 적합합니다.
설치가 필요 없고 파일 하나로 작동해서 개발 속도가 빠릅니다.
데이터가 수십만 건 이상 쌓이거나 동시 접속이 필요해지면
PostgreSQL로 마이그레이션할 계획입니다.
SQLAlchemy ORM을 쓰면 DB 변경 시 코드 수정을 최소화할 수 있습니다."

**Q. 랜덤포레스트를 선택한 이유는?**
"부동산 가격은 면적, 층, 건축년도, 위치 간의 관계가 비선형입니다.
예를 들어 같은 면적이라도 고층/저층에 따라 가격이 비선형으로 달라집니다.
랜덤포레스트는 이런 복잡한 관계를 잘 포착하고,
단일 의사결정나무보다 과적합에 강하며,
특성 중요도를 통해 '어떤 요인이 가격에 영향을 주는지' 직관적으로 확인할 수 있었습니다."

**Q. 자동화 파이프라인에서 가장 중요하게 생각한 것은?**
"멱등성입니다.
자동 수집 스케줄러가 매월 실행될 때
같은 데이터가 중복 저장되면 통계가 틀어집니다.
INSERT OR IGNORE + UNIQUE 제약으로 몇 번 실행해도
항상 동일한 결과가 보장되는 파이프라인을 설계했습니다."

**Q. 앞으로 개선하고 싶은 점은?**
"세 가지가 있습니다.
첫째, PostgreSQL 마이그레이션으로 대용량 데이터와 동시 접속 지원.
둘째, 전국 데이터로 확장해서 지역별 비교 분석 추가.
셋째, 가격 예측 모델에 외부 변수(기준금리, 주택담보대출 금리)를 추가해
예측 정확도를 높이고 싶습니다."

---

## 마무리 한 마디

> "이 프로젝트를 통해 데이터 엔지니어링의 전체 흐름을
> 직접 경험했습니다. 코드를 쓰는 것보다
> 어떤 구조로 만들어야 유지보수가 쉬운지,
> 자동화가 안정적으로 돌아가려면 어떤 설계가 필요한지를
> 실전에서 배웠다는 게 가장 큰 수확입니다."
