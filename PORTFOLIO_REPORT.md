# 서울 아파트 실거래가 자동화 파이프라인 — 포트폴리오 종합 리포트

> GitHub: https://github.com/spender1233-netizen/seoul-real-estate  
> 라이브 데모: https://seoul-real-estate-mer4p6ydwb8qyupff7eo33.streamlit.app/

---

## 1. 프로젝트 한 문장 요약

국토교통부 공공 API로 서울 아파트 실거래가 데이터를 자동 수집해서  
SQLite DB에 저장하고, 통계 분석·ML 예측·Streamlit 대시보드·GPT AI 해설까지  
**end-to-end 데이터 파이프라인을 혼자 설계하고 구현한 포트폴리오 프로젝트**

---

## 2. 전체 코드 흐름 (큰 틀)

```
[국토부 공공 API]
       ↓ HTTP 요청 / XML 파싱 / 페이징
[collectors/trade_collector.py]  ← 매매 수집
[collectors/rent_collector.py]   ← 전월세 수집
       ↓ pandas DataFrame
[main.py]  ← 지휘자 역할 (CLI 진입점)
       ↓ INSERT OR IGNORE
[database.py]  ← 테이블 설계 (UNIQUE 제약)
[db_save.py]   ← 중복 없는 저장 (멱등성)
       ↓ SQL 집계 쿼리
[analyzer.py]  ← 통계 분석 5종
[ml_model.py]  ← 랜덤포레스트 가격 예측
       ↓ import
[dashboard.py] ← Streamlit 대시보드 7탭
[kakao_map.py] ← Folium 지도 히트맵
[ai_analyst.py]← GPT-4o-mini AI 해설
       ↓ 자동 실행
[scheduler.py] ← 매월 1일 자동 수집 + 리포팅
[reporter.py]  ← 주간/월간/분기/연간 HTML 리포트
```

---

## 3. 파일별 역할 · 연계 관계 · 사용 이유

### config.py — 설정값 모음
- **역할**: 서울 25개 구 법정동코드, API 상수, 요청 간격 관리
- **사용 이유**: 코드에 숫자를 직접 쓰면(하드코딩) 나중에 바꿀 때 전체를 뒤져야 함. 한 곳에 모으면 여기만 수정하면 전체 반영
- **연계**: 모든 파일이 `from config import`로 가져다 씀

### collectors/ — 데이터 수집
- **역할**: 국토부 API 호출 → XML 파싱 → DataFrame 반환
- **핵심 설계**:
  - 페이징 처리: `while True`로 totalCount 도달까지 100건씩 반복
  - 지수 백오프: 실패 시 2초→4초→8초 대기 후 재시도
  - 타입 힌트 + docstring 적용 (코드 품질)
- **사용 이유**: API가 한 번에 100건만 반환하므로 자동 페이징 필수. 네트워크 불안정에 대비한 재시도 로직 필요
- **연계**: `main.py`가 호출 → 결과를 `db_save.py`에 전달

### database.py — DB 스키마 정의
- **역할**: SQLite 테이블 생성, 인덱스, UNIQUE 제약 설정
- **핵심 설계**:
  ```sql
  UNIQUE(apt_name, area, floor, trade_date, price)
  ```
  같은 거래가 두 번 들어오면 자동으로 거부
- **사용 이유**: IF NOT EXISTS로 몇 번 실행해도 안전. UNIQUE 제약이 중복 방지의 핵심
- **연계**: `main.py`가 시작 시 `init_db()` 호출

### db_save.py — 저장 로직
- **역할**: DataFrame → DB 저장 (INSERT OR IGNORE)
- **핵심 설계**:
  ```python
  # 중복이면 조용히 스킵, 새 데이터만 저장
  INSERT OR IGNORE INTO apt_trade (...) VALUES (...)
  ```
- **사용 이유**: 멱등성(Idempotency) — 1000번 실행해도 결과가 같음. 자동화 파이프라인의 핵심 원칙
- **연계**: `main.py` → `save_trade()` → DB

### analyzer.py — 통계 분석
- **역할**: DB에서 데이터 꺼내서 5종 분석 수행
  1. 구별/지역별 평균·최저·최고가
  2. 월별 가격 추세 (전월 대비 등락률)
  3. 면적 구간별 ㎡당 단가
  4. 단지별 거래 히스토리
  5. Z-score 기반 이상거래 탐지 (2.5σ 기준)
- **사용 이유**: SQL로 DB 안에서 계산 후 결과만 가져오면 메모리 효율 극대화. Z-score는 통계적으로 검증된 이상치 탐지 방법
- **연계**: `dashboard.py`와 `ai_analyst.py`가 모두 import해서 사용

### ml_model.py — 가격 예측 ML
- **역할**: 구·면적·층·건축년도 입력 → 예측 매매가 + 신뢰 범위 출력
- **핵심 설계**:
  - LabelEncoder로 텍스트("강남구") → 숫자 변환
  - building_age = 현재연도 - 건축년도 (연식이 가격에 영향)
  - RandomForestRegressor(n_estimators=200)
  - 200개 나무의 표준편차 → 예측 불확실성 범위
- **사용 이유**: 부동산 가격은 비선형 관계. 랜덤포레스트는 과적합에 강하고 특성 중요도로 해석 가능
- **연계**: `dashboard.py` 🤖 가격 예측 탭에서 호출

### dashboard.py — 대시보드
- **역할**: 분석 결과를 브라우저에서 인터랙티브하게 시각화
- **7개 탭**:
  - 📈 가격 추세 / 🗺️ 지역 분석 / 📐 면적별 단가
  - 🔎 단지 검색 / ⚠️ 이상거래 / 🗺️ 지도 히트맵 / 🤖 가격 예측
- **사용 이유**: Streamlit은 HTML/CSS/JS 없이 Python만으로 웹앱 구현 가능. 빠른 프로토타이핑에 최적
- **연계**: analyzer, ml_model, kakao_map, ai_analyst를 모두 import해서 탭으로 조립. dashboard는 "화면 조립"만 담당

### kakao_map.py — 지도 히트맵
- **역할**: 서울 25개 구 평균가를 지도 위에 파랑→빨강 색상으로 시각화
- **사용 이유**: 카카오맵 JS SDK는 Streamlit iframe 보안에 차단됨 → Folium으로 대체. Python에서 직접 HTML 생성하므로 외부 API 의존 없음
- **연계**: `dashboard.py` 지도 히트맵 탭에서 호출

### ai_analyst.py — AI 해설
- **역할**: 분석 데이터 → GPT 프롬프트 → 전문가 수준 한국어 시장 분석
- **핵심 설계**:
  - 20년 경력 부동산 애널리스트 시스템 프롬프트
  - 실거래 수치를 프롬프트에 직접 주입 (할루시네이션 방지)
  - temperature=0.3 (낮을수록 일관되고 정확한 답변)
  - 스트리밍으로 실시간 출력 (yield)
- **사용 이유**: AI에게 맥락(실제 수치)을 주면 일반적인 답변이 아닌 데이터 기반 분석 가능
- **연계**: `dashboard.py` AI 분석 버튼 → `stream_analysis()` → 실시간 출력

### scheduler.py + reporter.py — 자동화
- **역할**: 정해진 시간에 자동 수집 + HTML 리포트 생성
- **스케줄**:
  - 매일 06:00 → 주요 5개 구 보완 수집
  - 매주 월 07:00 → 주간 리포트
  - 매월 1일 00:05 → 서울 전체 수집
  - 매월 1일 07:30 → 월간 리포트
  - 분기 첫날 08:00 → 분기 리포트
  - 1월 1일 09:00 → 연간 리포트
- **사용 이유**: 진짜 자동화. 한 번 실행해두면 손을 안 대도 데이터가 쌓이고 리포트가 생성됨

---

## 4. 핵심 설계 원칙 3가지

### ① 관심사 분리 (Separation of Concerns)
수집·저장·분석·시각화·AI를 각각 별도 파일로 분리.  
지도 로직을 바꿔도 분석 코드를 건드릴 필요 없음.  
각 파일은 자기 역할만 담당.

### ② 멱등성 (Idempotency)
`INSERT OR IGNORE + UNIQUE 제약`으로 같은 데이터를 1000번 저장해도 결과가 동일.  
자동화 파이프라인에서 가장 중요한 설계 원칙.  
실수로 중복 실행해도 데이터가 망가지지 않음.

### ③ 단계별 구현 (Incremental Development)
1단계가 작동하면 2단계, 이렇게 순서대로 진행.  
처음부터 완성본을 만들려 하지 않음.  
각 단계의 성취감이 동기부여가 되어 6단계까지 완성.

---

## 5. 지원 가능한 공고

### 데이터 엔지니어 (주니어)
- **어필 포인트**: 공공 API 수집 → DB 설계 → 파이프라인 자동화 전체 경험
- **강조할 것**: 멱등성 파이프라인, SQLite 스키마 설계, 스케줄러 구현
- **관련 기업**: 부동산 플랫폼(직방, 다방, 호갱노노), 핀테크, 데이터 스타트업

### 데이터 분석가 (주니어)
- **어필 포인트**: SQL 집계, pandas, Z-score 이상치 탐지, 시각화
- **강조할 것**: 5종 분석 지표, 실거래 데이터 기반 인사이트 도출
- **관련 기업**: 부동산 리서치, 금융사 데이터팀, 컨설팅

### Python 백엔드 개발자 (주니어)
- **어필 포인트**: API 연동, DB 설계, 자동화, 배포
- **강조할 것**: 재시도 로직, 에러 핸들링, Streamlit Cloud 배포
- **관련 기업**: 스타트업 백엔드, SaaS 회사

### AI/LLM 엔지니어 (주니어)
- **어필 포인트**: GPT API 연동, 프롬프트 엔지니어링, 스트리밍 구현
- **강조할 것**: 전문가 페르소나 시스템 프롬프트, 데이터 기반 AI 해설
- **관련 기업**: AI 스타트업, LLM 서비스 기업

---

## 6. 포트폴리오 작성 가이드

### 이력서/포트폴리오에 쓸 한 줄 요약
> "국토부 공공 API 기반 서울 아파트 실거래가 자동화 파이프라인 구축  
> (수집 38,995건 / SQLite / Streamlit / ML 가격 예측 / GPT AI 해설 / 자동 배포)"

### 프로젝트 설명 섹션 (200자 버전)
> 국토교통부 실거래가 API에서 서울 25개 구 38,995건 데이터를 자동 수집해
> SQLite DB에 멱등성 있게 저장하고, 통계 분석·랜덤포레스트 가격 예측·
> Streamlit 대시보드·GPT-4o-mini AI 시장 해설까지 end-to-end로 구현.
> APScheduler 기반 자동 수집 스케줄러와 주간/월간/분기/연간 HTML 리포트 자동 생성.
> Streamlit Cloud에 배포, 유닛 테스트 20개 작성.

### 기술 스택 표기
```
Python 3.12 / SQLite / pandas / scikit-learn / Streamlit / Plotly / Folium
OpenAI API / APScheduler / Git/GitHub / Streamlit Cloud
```

---

## 7. 면접 스크립트

### 30초 엘리베이터 피치
> "서울 아파트 실거래가를 국토부 공공 API로 자동 수집해서
> SQLite DB에 저장하고, 통계 분석과 ML 가격 예측, Streamlit 대시보드,
> GPT 기반 AI 시장 해설까지 end-to-end로 혼자 구현한 데이터 파이프라인입니다.
> 현재 서울 25개 구 38,995건 데이터가 쌓여 있고,
> 매월 1일 자동 수집·리포팅되며 Streamlit Cloud에 배포되어 있습니다."

---

### 왜 만들었나?
> "부동산 가격이 궁금할 때마다 여러 사이트를 수동으로 확인해야 했습니다.
> 국토부에서 실거래가를 공공 API로 무료 제공한다는 걸 알고,
> 직접 수집해서 내가 원하는 방식으로 분석하고 싶었습니다.
> 처음부터 완성본을 만들지 않고 1단계(수집)부터 6단계(배포)까지
> 단계별로 작동하는 걸 확인하며 진행했습니다."

---

### 어떤 문제를 해결했나?

**문제 1. API 문서와 실제 동작의 불일치**
> "공공 API 문서에는 http로 나와 있었지만 실제로는 https만 허용.
> XML 태그명도 문서(한글)와 응답(영문 aptNm, dealAmount)이 달랐습니다.
> 브라우저로 직접 API를 호출해서 실제 응답 구조를 확인하고 수정했습니다."

**문제 2. 중복 데이터 저장**
> "자동 스케줄러가 반복 실행될 때 같은 거래가 중복 저장되는 문제.
> UNIQUE 제약 조건과 INSERT OR IGNORE를 조합해서
> 몇 번 실행해도 항상 동일한 결과가 보장되는 멱등성 파이프라인을 설계했습니다."

**문제 3. Streamlit iframe 보안 차단**
> "카카오맵 JavaScript SDK가 Streamlit의 iframe 보안 정책에 차단됨.
> Folium으로 대체해서 Python에서 직접 HTML을 생성하는 방식으로
> 외부 API 의존 없이 지도 히트맵을 구현했습니다."

---

### 무엇을 배웠나?

**기술적 배움**
> "데이터 파이프라인 설계의 전체 흐름을 직접 경험했습니다.
> 특히 멱등성 개념 — 몇 번 실행해도 결과가 같아야 한다는 원칙이
> 자동화 시스템에서 얼마나 중요한지 실전으로 배웠습니다.
> 관심사 분리 원칙 덕분에 각 파일을 독립적으로 수정할 수 있었고,
> 유닛 테스트 20개를 작성하며 코드 신뢰성을 높이는 방법을 익혔습니다."

**도메인 배움**
> "서울 아파트 시장에서 강남·서초·송파가 평균 20억 이상인 반면
> 노원·도봉은 5억 이하로 같은 서울 안에서도 4배 이상 격차가 있습니다.
> 40-60㎡ 소형 아파트의 ㎡당 단가가 대형보다 높다는 것도 데이터로 확인했습니다."

**개발 철학**
> "처음부터 완벽하게 만들려 했다면 중간에 포기했을 것 같습니다.
> 작동하는 단계를 하나씩 쌓는 방식이 가장 현실적이고 효과적이었습니다."

---

### 예상 면접 Q&A

**Q. SQLite를 선택한 이유는?**
> "프로젝트 초기엔 SQLite가 적합합니다. 설치 없이 파일 하나로 작동해서 개발이 빠릅니다.
> 데이터가 수십만 건 이상이거나 동시 접속이 필요하면 PostgreSQL로 마이그레이션할 계획입니다.
> ORM을 쓰면 DB 변경 시 코드 수정을 최소화할 수 있습니다."

**Q. 랜덤포레스트를 선택한 이유는?**
> "부동산 가격은 면적·층·건축년도 간 관계가 비선형입니다.
> 랜덤포레스트는 이런 복잡한 관계를 잘 포착하고, 과적합에 강하며,
> 특성 중요도로 '어떤 요인이 가격에 영향을 주는지' 직관적으로 확인할 수 있었습니다."

**Q. 멱등성이란?**
> "같은 작업을 여러 번 실행해도 결과가 항상 동일한 성질입니다.
> INSERT OR IGNORE + UNIQUE 제약으로 구현했습니다.
> 자동화 파이프라인에서 중복 실행이 데이터를 망가뜨리지 않도록 하는 핵심 설계 원칙입니다."

**Q. 가장 어려웠던 점은?**
> "API 문서와 실제 동작이 달랐던 것입니다.
> http→https 이슈, XML 태그 불일치, Streamlit iframe 보안 제한 등
> 문서만으로는 알 수 없는 문제들이 있었고,
> 각각을 직접 테스트하며 해결했습니다. 실전 경험의 가장 큰 배움이었습니다."

**Q. 앞으로 개선하고 싶은 점은?**
> "세 가지입니다.
> 첫째, PostgreSQL 마이그레이션으로 대용량 지원.
> 둘째, 전국 데이터로 확장해서 지역별 비교 분석 추가.
> 셋째, 기준금리·대출금리 외부 변수를 ML 모델에 추가해 예측 정확도 향상."

---

## 8. 프로젝트 수치 요약 (포트폴리오 강조 포인트)

| 항목 | 수치 |
|------|------|
| 수집 데이터 | 38,995건 (서울 25개 구 × 6개월) |
| 분석 지표 | 5종 (구별/지역별/면적/추세/이상거래) |
| 대시보드 | 7개 탭 |
| ML 모델 | RandomForest 200 trees |
| 테스트 | 유닛 테스트 20개 통과 |
| 자동화 | 4종 주기 리포트 (주간/월간/분기/연간) |
| 배포 | Streamlit Cloud 라이브 |

---

*이 문서는 면접 준비, 포트폴리오 작성, 자기소개서에 활용하세요.*
